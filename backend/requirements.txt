# FastAPI Backend Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
requests>=2.31.0
tqdm>=4.66.0
boto3>=1.28.0
kaggle>=1.5.16
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# CORS is handled by FastAPI's built-in CORSMiddleware (no extra package needed)

# Data Processing & ML
# Using versions compatible with Python 3.10-3.12
# Note: pandas 2.1.3 does NOT support Python 3.13
# If Render uses Python 3.13, upgrade to pandas 2.2.0+ or pin Python to 3.10-3.12
# Using newer pandas version that supports Python 3.13 as fallback
pandas>=2.2.0
numpy>=1.26.2
scikit-learn>=1.3.2

# WebSocket support for real-time updates
websockets==12.0

# Environment variables
python-dotenv==1.0.0

# Optional: For production deployment
gunicorn==21.2.0

